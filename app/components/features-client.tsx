"use client";import {useState} from "react";import SiteNav from "./site-nav";import SiteFooter from "./site-footer";
const initial=[["End-to-end encryption",148],["Folders in shared sessions",96],["Custom transfer branding",73],["Desktop menu-bar app",61]] as const;
export default function FeaturesClient(){const [votes,setVotes]=useState<Record<string,number>>({});return <div className="flex min-h-screen flex-col"><SiteNav/><main className="flex-1 px-6 py-20"><div className="mx-auto max-w-5xl"><p className="font-mono-tight text-xs uppercase tracking-widest text-primary">Have a Say</p><h1 className="mt-3 text-6xl font-bold uppercase tracking-tighter">What should come next?</h1><p className="mt-5 max-w-2xl text-muted-foreground">This preserved voting board is interactive but local. Votes reset when you leave.</p><div className="mt-12 grid gap-4 sm:grid-cols-2">{initial.map(([name,count])=><article key={name} className="flex items-center justify-between border border-border bg-card p-6"><div><h2 className="text-lg font-bold">{name}</h2><p className="mt-1 font-mono-tight text-[10px] uppercase text-muted-foreground">{count+(votes[name]||0)} votes</p></div><button onClick={()=>setVotes(v=>({...v,[name]:v[name]?0:1}))} className={`industrial-button ${votes[name]?"industrial-button-dark":"industrial-button-outline"}`}>{votes[name]?"Voted":"Vote"}</button></article>)}</div></div></main><SiteFooter/></div>}
